import 'package:flutter/cupertino.dart';

const Color APP_COLOR = Color.fromARGB(255, 218, 59, 254);
const Color BTN_COLOR = Color.fromARGB(255, 59, 254, 130);

const int PAGE_LIMIT = 10;

enum SortTypes {
  ASC,
  DESC,
}

enum GetTypes {
  FILTER,
  PAGING,
}
